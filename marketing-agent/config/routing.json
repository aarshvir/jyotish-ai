{
  "_comment": "brain() routing. Each tier lists CLIs in preference order; the router tries them in turn, skipping disabled/rate-limited ones. Spreads load to respect each subscription's fair-use. Edit freely — no restart needed (read per call).",
  "killSwitchFile": "data/KILL",
  "tiers": {
    "bulk":  { "clis": ["gemini", "codex", "claude"] },
    "smart": { "clis": ["claude", "codex", "gemini"] },
    "code":  { "clis": ["codex", "gemini"] }
  },
  "clis": {
    "gemini": {
      "enabled": false,
      "_disabled_why": "2026-07-25: Google dropped free-tier Code Assist for this client (IneligibleTierError) — every bulk call wasted ~12s failing before falling through. Re-enable after re-auth.",
      "model": "gemini-2.5-flash",
      "dailyCap": 1000,
      "minIntervalMs": 1500,
      "timeoutMs": 120000
    },
    "codex": {
      "enabled": true,
      "model": null,
      "dailyCap": 300,
      "minIntervalMs": 3000,
      "timeoutMs": 180000
    },
    "claude": {
      "enabled": "auto",
      "model": null,
      "dailyCap": 200,
      "minIntervalMs": 2000,
      "timeoutMs": 180000
    }
  }
}
