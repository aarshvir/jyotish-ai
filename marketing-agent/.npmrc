# npm >=11 blocks install scripts by default. These two are load-bearing: better-sqlite3 compiles
# the native addon (lessons store + spend ledger) and playwright fetches Chromium (product screencaps).
allow-scripts=better-sqlite3,playwright
